# General Rules
- Never run the app unless if i ask directly
- Never give me a long summary of what you did unless if i ask directly
- Never guess
- Never overthink
- Always follow clean code rules by Uncle Bob for separation of concepts, hooks, utils, small units, folder structure and hierarchy
- Always use tailwind classes for things and avoid inline styling
- Never do inline blocks of code and make utils instead and do not mix jsx with business logic and such
- Never hardcode styles and never use px and such, always use css tokens from tailwind